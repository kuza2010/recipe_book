<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:name=".BaseApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/AppThemeBlueBerry"
        android:supportsRtl="true"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <!--<activity-->
            <!--android:name=".presentation.ui.login.Main"-->
            <!--android:label="@string/app_name">-->

            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.MAIN" />-->
                <!--<category android:name="android.intent.category.LAUNCHER" />-->
            <!--</intent-filter>-->

            <!--<intent-filter>-->
                <!--<action android:name="android.intent.action.SEARCH" />-->
            <!--</intent-filter>-->

            <!--<meta-data-->
                <!--android:name="android.app.searchable"-->
                <!--android:resource="@xml/searchable"/>-->

        <!--</activity>-->

        <!--<activity android:name=".presentation.ui.login.RecordActivity"-->
            <!--android:theme="@android:style/Theme.NoTitleBar" />-->

        <!--<provider android:name=".content_provider.SearchProvider"-->
            <!--android:authorities="com.example.myapplication.content_provider.SearchProvider" />-->

        <!--<meta-data android:name="android.app.default_searchable"-->
            <!--android:value=".Main" />-->

        <activity
            android:name=".presentation.ui.login.LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.ui.main.MainActivity"
            android:label="@string/app_name">

        </activity>

        <activity android:name=".presentation.ui.recipe.RecipeActivity" />

    </application>

</manifest>